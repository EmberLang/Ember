struct Point {
    x: i32,
    y: i32
}

struct Box<T> {
    value: T,
    tag: i32
}

fn main() {
    let named = .Point{ x = 1, y = 2 };
    let nested = .Box<Box<i32>>{ value = .{ value = 3, tag = 4 }, tag = 5 };
    if .Point{ x = 6, y = 7 }.x == 6 {}
}
